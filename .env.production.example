# ─────────────────────────────────────────────────────────────────
# OrbitFlow CRM - Production Environment Variables
# ─────────────────────────────────────────────────────────────────

# ──── DATABASE ────────────────────────────────────────────────────
# MariaDB/MySQL connection string
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# Example: mysql://orbitflow:SecurePass123@db.example.com:3306/orbitflow_prod
DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/DATABASE"

# ──── AUTHENTICATION ──────────────────────────────────────────────
# Generate secure secret with: openssl rand -base64 32
# CRITICAL: Use a unique secret for production (minimum 32 characters)
# NEVER reuse development secrets in production
AUTH_SECRET="REPLACE_WITH_SECURE_RANDOM_STRING_MIN_32_CHARS"

# Full URL where the application is deployed
# Must include protocol (https://) and domain
# Example: https://crm.yourdomain.com
# IMPORTANT: Must be HTTPS in production (HTTP is insecure)
AUTH_URL="https://your-domain.com"

# Required for reverse proxy setups (Coolify, Nginx, Caddy, Traefik)
# Allows NextAuth to trust the X-Forwarded-Host header
AUTH_TRUST_HOST=true

# ──── OPTIONAL FEATURES ───────────────────────────────────────────
# Facebook Pixel ID (for quiz conversion tracking)
# Uncomment and add your Pixel ID if using Facebook Ads
# NEXT_PUBLIC_FACEBOOK_PIXEL_ID=""

# TikTok Pixel ID (for quiz conversion tracking)
# Uncomment and add your Pixel ID if using TikTok Ads
# NEXT_PUBLIC_TIKTOK_PIXEL_ID=""

# Custom timezone for date handling (defaults to server timezone)
# Example: America/New_York, Europe/London, Asia/Dubai
# TZ="America/New_York"

# ─────────────────────────────────────────────────────────────────
# SECURITY CHECKLIST:
# ─────────────────────────────────────────────────────────────────
# [ ] Generate NEW AUTH_SECRET (never reuse from development)
# [ ] Verify .env is in .gitignore (never commit to version control)
# [ ] Use HTTPS for AUTH_URL (never HTTP in production)
# [ ] Use strong database credentials (16+ characters, mixed case, symbols)
# [ ] Enable SSL/TLS for database connections
# [ ] Set DATABASE_URL with production database (not localhost)
# [ ] Change all default seeded passwords before going live
# [ ] Verify AUTH_TRUST_HOST=true for reverse proxy setups
# [ ] Review Prisma schema for sensitive fields
# [ ] Enable rate limiting on public endpoints (/q/...)
# ─────────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────────
# DEPLOYMENT NOTES:
# ─────────────────────────────────────────────────────────────────
# 1. For Coolify: Set these variables in the Coolify dashboard
# 2. For Vercel: Set in Project Settings → Environment Variables
# 3. For Railway: Set in Variables tab
# 4. For VPS: Create .env file (ensure NOT committed to git)
# 5. For Docker: Pass as -e flags or use docker-compose.yml
# ─────────────────────────────────────────────────────────────────
